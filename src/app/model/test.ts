export class Test {
    id: number;
    label: string;
    re:string;

    constructor(id:number, label:string, re:string){
        this.id = id;
        this.label = label;
        this.re = re;
    }
}
